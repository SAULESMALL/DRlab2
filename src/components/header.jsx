// Define the Header component using an arrow function
const Header = () => {

  // JSX to render UI elements
  return (
    <div>
      {/* Heading text displayed on the page */}
      <h1>Hello from Header Component!</h1>
    </div>
  );
}

// Export the Header component so it can be imported and used in other files (like App.jsx)
export default Header;