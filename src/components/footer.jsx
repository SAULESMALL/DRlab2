// Define the Footer component using an arrow function
const Footer = () => {

  // JSX to render the footer UI
  return (
    <div>
      {/* Footer heading displayed on the page */}
      <h1>Hello from Footer Component!</h1>
    </div>
  );
}

// Export the Footer component so it can be imported and used in other files (like App.jsx)
export default Footer;