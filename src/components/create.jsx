// Define the Create component as an arrow function
const Create = () => {
  // JSX returned by the component
  return (
    <div>
      {/* Heading displayed for the Create component */}
      <h1>Hello from Create component</h1>
    </div>
  );
}

// Export the Create component so it can be imported into App.jsx or other components
export default Create;